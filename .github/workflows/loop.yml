name: make-loop
on:
  workflow_dispatch:
jobs:
  loop:
    strategy:
      matrix:
        solutions: [FSK_Webresources, FSK_Entities, FSK_Plugins, FSK_Processes]
    runs-on: [ubuntu-latest]
    steps:
      - name: deploy
        run: |
          echo ${{ matrix.solutions }} oki

