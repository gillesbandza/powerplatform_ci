name: make-loop
on:
  workflow_dispatch:
jobs:
  install:
    strategy:
      matrix:
        solutions: [FSK_Webresources, FSK_Entities, FSK_Plugins, FSK_Processes]
    runs-on: [ubuntu-latest]
    steps:
      - name: deploy
        run: |
          echo Deploying solution ${{ matrix.solutions }} !

